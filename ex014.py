# Escreva um programa que converta uma temperatura digitada em °C para °F.
temp_c = float(input('Informe a temperatura atual: '))
temp_f = (temp_c * 1.8 + 32)
print(f'A temperatura atual de {temp_c} corresponde a {temp_f}')
